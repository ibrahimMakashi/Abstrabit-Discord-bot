# Frontend environment (copy to frontend/.env)
#
# Optional for local development — Vite proxies /api and /socket.io to the backend
# when this is unset or points at localhost:5000 (see frontend/vite.config.js).

VITE_API_BASE_URL=http://localhost:5000/api
